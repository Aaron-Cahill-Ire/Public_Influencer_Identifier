# Public Influencer Identifier

A small starter project for identifying influential public accounts.

## Quick start

```bash
python influencer_identifier.py
```

This prints the top-ranked accounts from a sample dataset using a naive
influence heuristic (follower reach weighted by engagement). The scoring
logic in `influencer_identifier.py` is placeholder scaffolding meant to be
replaced with real data sources and a proper model.
